# Production-only requirements for the FastAPI backend.
# Strips out offline-only tools (streamlit, plotly, bert-score, datasets)
# that are heavy and unused at runtime.

# Core RAG
langchain
langchain-core
langchain-text-splitters
langchain-community
langchain-huggingface
transformers==4.36.2
sentence-transformers==2.7.0
faiss-cpu
pyyaml

# Web API
fastapi
uvicorn[standard]
